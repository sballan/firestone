1. Create data structure Models in Mongo
	- Array, Hash, String(, Path?)
2. API to return JSON at a path
	- Parse the JSON, create/find appropriate data structures
	- models should memoize their responses